include _mixins
doctype html
html(lang='en')
  head
    include _head
  body#skrollr-body(class=bodyClass)
    //BEGIN out
    .out(class=outClass)
      block content
      block pop-up
        include ../partials/_pop-up
    //END out

    //LOAD SCRIPTS
    block scripts
      script(type="text/javascript" src="js/wow.min.js")
      script.
        //wow;
        new WOW().init({
          live: true,
        });
      script(src="https://unpkg.com/swiper/js/swiper.min.js")
      script(type="text/javascript" src="js/app.js")
